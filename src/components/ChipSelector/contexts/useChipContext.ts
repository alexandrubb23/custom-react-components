import useRequiredContext from '../../../hooks/useRequiredContext';
import ChipContext from './ChipContext';

const useChipContext = () => useRequiredContext(ChipContext);

export default useChipContext;
